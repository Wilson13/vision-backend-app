{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "meet-queue-chart.serviceAccountName" . }}
  labels:
{{ include "meet-queue-chart.labels" . | nindent 4 }}
{{- end -}}
