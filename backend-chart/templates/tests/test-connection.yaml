apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meet-queue-chart.fullname" . }}-test-connection"
  labels:
{{ include "meet-queue-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "meet-queue-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
